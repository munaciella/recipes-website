{
    "version": 2,
    "env": {
      "MONGODB_URI": "@mongodb_uri"
    },
    "build": {
      "env": {
        "MONGODB_URI": "@mongodb_uri"
      }
    },
    "routes": [
      {
        "src": "/api/(.*)",
        "dest": "/api/$1"
      }
    ]
  }